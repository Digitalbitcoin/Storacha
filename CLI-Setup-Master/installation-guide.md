# 📖 Complete Installation Guide

## Prerequisites Check

### Step 1: Verify Node.js and npm Versions
```bash
# Check Node.js version (must be 18 or higher)
node --version

# Check npm version (must be 7 or higher)
npm --version

# Alternative: Check both at once
node --version && npm --version
